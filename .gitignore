# Per-DB configs contain secrets — never check in.
/dbapps/*.json

# Python cache
__pycache__/
*.pyc
.pytest_cache/

# Local logs (in case anything ends up in the repo root)
*.log

# OS noise
.DS_Store
.Trash-*/

# Editor noise
.vscode/
.idea/

# Domino crawl output contains plaintext env vars, PATs, credentials — never check in.
MRM-Portal-main/crawl_output/
